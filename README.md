A dice app that uses the imported random module, various functions, and a loop 
to write a program that will roll a given number of dice of any number of sides and sum the total
until the user decides to exit
